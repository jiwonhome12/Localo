# 협업 가이드

1. `main`에는 **직접 push 금지.**
2. 작업할 때마다 `main`에서 **새 브랜치** 생성.
3. 커밋 메시지는 `타입: 한글 요약` 형태로 써요. ex) `feat: 지역 카드 컴포넌트 추가` + 디테일한 설명 추가도 ㄱㄴ.
4. 작업이 끝나면 **Pull Request(PR)** 를 만들고, **팀장 승인**을 받은 뒤 병합 가능.
5. `.env` 파일, API 키 업로드 금지.

\---

## 1\. 자주 나오는 용어 

|용어|뜻|
|-|-|
|clone|GitHub의 프로젝트를 내 컴퓨터로 통째로 내려받기|
|branch|원본(`main`)을 건드리지 않고 작업하기 위한 나만의 작업 공간|
|commit|작업 내용을 한 단계씩 저장하는 것 (게임의 세이브 포인트)|
|push|내 컴퓨터의 커밋을 GitHub에 올리기|
|pull|GitHub의 최신 내용을 내 컴퓨터로 가져오기|
|PR (Pull Request)|"내 브랜치 작업을 main에 합쳐주세요"라는 요청|
|merge|브랜치의 작업을 main에 합치기|

\---

## 2\. 브랜치 전략

복잡한 전략 대신 가장 단순한 **GitHub Flow**를 사용.

```
main  ──●──────●──────────●────────●──   (항상 동작하는 상태, 직접 push 금지)
         \\    /  \\       /
          feature/region-list   feature/festival-calendar   ← 작업 브랜치
```

* `main`: 항상 실행 가능한 상태를 유지. 배포도 `main` 기준.
* 작업 브랜치: 기능 하나, 버그 하나마다 `main`에서 새로 만들고, 병합되면 삭제.





### 브랜치 이름 규칙

`타입/영어-소문자-설명` 형태로 작성. 한글과 공백은 쓰지 않음.

|타입|언제|예시|
|-|-|-|
|`feature/`|새 기능 만들 때|`feature/region-list`, `feature/festival-calendar`|
|`fix/`|버그 고칠 때|`fix/calendar-date-error`|
|`docs/`|문서만 수정할 때|`docs/update-readme`|
|`chore/`|설정, 패키지 등|`chore/add-tailwind`|

### 

### 하루 작업 순서

```bash
# 1. main을 최신으로 만들기
git checkout main
git pull origin main

# 2. 내 작업 브랜치 만들기
git checkout -b feature/region-list

# 3. 작업하기 (코드 작성)

# 4. 뭐가 바뀌었는지 확인하기 (중요!)
git status

# 5. 저장하기
git add .
git commit -m "feat: 지역 카드 컴포넌트 추가"

# 6. GitHub에 올리기
git push origin feature/region-list
```

그다음 GitHub 레포 페이지에서 **Compare \& pull request** 버튼을 눌러 PR을 만들기. 
(PR 작성법은 4번에)

병합된 뒤에는 이렇게 정리.

```bash
git checkout main
git pull origin main
git branch -d feature/region-list
```

### 작업 중에 main이 바뀌었다면

다른 팀원의 작업이 먼저 병합되었을 수 있어요. PR을 올리기 전에 내 브랜치에 최신 `main`을 합쳐 주세요.

```bash
git checkout main
git pull origin main
git checkout feature/region-list
git merge main
```

### 충돌(conflict)이 났다면

같은 파일의 같은 부분을 두 사람이 고치면 충돌이 남. 

1. 충돌 난 파일을 열면 `<<<<<<<`, `=======`, `>>>>>>>` 표시가 보임.
2. 어느 쪽 코드를 남길지 정하고, 표시들을 지우기.
3. `git add .` → `git commit`으로 마무리.

\---

## 3\. 커밋 컨벤션

### 형식

```
타입: 한글로 짧게 요약
```

### 타입 종류

|타입|언제 쓰나요|
|-|-|
|`feat`|새 기능 추가|
|`fix`|버그 수정|
|`docs`|문서 수정 (README 등)|
|`style`|줄바꿈·들여쓰기 같은 코드 모양 정리 (동작은 그대로)|
|`refactor`|동작은 같고 코드 구조만 개선|
|`test`|테스트 코드 추가·수정|
|`chore`|설정, 패키지 설치 등 기타 작업|

> 화면 디자인(CSS, Tailwind) 수정은 `style`이 아니라 `feat`나 `fix`로 써요. `style`은 코드 정리에만 사용.

### 작성 규칙

* **한 커밋에는 한 가지 일만** .
* 요약은 **50자 이내**, 끝에 마침표는 찍지 않음.
* 무엇을 했는지가 보이게 작성. ("수정", "작업"처럼 뭉뚱그리지 않기)



### 좋은 예 / 나쁜 예

|나쁜 예|좋은 예|
|-|-|
|`수정`|`fix: 축제 캘린더 월 이동 버튼 오류 수정`|
|`작업중`|`feat: 지역 카드 목록 화면 추가`|
|`asdf`|`chore: Tailwind CSS 설치`|
|`기능 추가하고 버그도 고치고 README도 수정`|세 개의 커밋으로 나누기|

\---

## 4\. Pull Request(PR) 규칙

### PR 만들기

* **제목**은 커밋 컨벤션과 같은 형식으로 써요. 예: `feat: 지역 카드 목록 화면 추가`
* **리뷰어(Reviewers)** 는 팀장을 지정.
* 아래 템플릿을 복사해서 본문에 채워주세요.

```markdown
## 작업 내용
- 무엇을 만들었는지 간단히 적어주세요.

## 확인 방법
- 리뷰하는 사람이 어떻게 실행해서 확인하면 되는지 적어주세요.
  예) npm run dev → /regions 페이지 접속

## 스크린샷
- 화면이 바뀌었다면 캡처를 붙여주세요.

## 체크리스트
- \[ ] 내 컴퓨터에서 실행해서 정상 동작을 확인했어요
- \[ ] .env 파일이나 API 키가 포함되지 않았어요
- \[ ] 콘솔에 에러가 없어요
```

### PR은 작게

* 하루\~이틀 분량의 작업으로 나눠서 올리기. PR이 작을수록 리뷰가 빠르고, 버그도 빨리 찾을 수 있음.
* 다 만들고 한 번에 올리기보다 **자주, 작게** 올려주세요.



### 리뷰와 병합

1. 팀장이 코드를 보고 코멘트를 남기거나 승인(Approve).
2. 코멘트가 있으면 **같은 브랜치에서 고쳐서 push**하면 PR에 자동으로 반영. 새 PR을 만들 필요가 없음.
3. **승인을 받은 뒤**에 작성자가 병합해요. Merge 버튼 옆 화살표(▼)에서 **Squash and merge**를 선택.
4. 병합 후에는 **Delete branch** 버튼으로 브랜치를 삭제.

\---

## 5\. 주간 정산 QA

일단 주에 한번 봐야되는데 시간 맞춰봅시다. 



### 진행 순서

1. **시연**: 각자 이번 주에 만든 기능을 화면에서 직접 보여주기.
2. **테스트**: 팀장이 체크리스트에 따라 기능을 눌러보며 확인. 팀원들도 같이 눌러보셈.
3. **이슈 등록**: 발견한 버그는 GitHub **Issues**에 등록.
4. **수정**: 담당자가 다음 세션 전까지 고쳐요.



### 버그 이슈 작성법

* **제목**: `\[bug] 화면/기능 - 증상` (예: `\[bug] 축제 캘린더 - 12월로 넘어가면 화면이 비어요`)
* **본문**: 아래 형식으로 써요.

```markdown
## 어떤 문제인가요?
증상을 한 줄로 적어주세요.

## 재현 방법
1. 어디로 들어가서
2. 무엇을 눌렀더니
3. 이런 일이 생겼어요

## 기대했던 동작
원래는 이렇게 되어야 해요.

## 스크린샷
(있다면 붙여주세요)
```

* **라벨**은 `bug`를 붙이고, **담당자(Assignees)** 를 지정.
* 버그를 고치는 PR 본문에 `Closes #이슈번호`를 적으면 병합될 때 이슈가 자동으로 닫힘.



### QA 체크리스트 (기본)

* \[ ] 페이지가 에러 없이 열리나요?
* \[ ] 버튼·링크를 눌렀을 때 의도한 대로 동작하나요?
* \[ ] 데이터가 없을 때(빈 목록)와 로딩 중, 에러가 났을 때 화면이 자연스러운가요?
* \[ ] 모바일 화면 크기에서도 깨지지 않나요?
* \[ ] 콘솔(F12)에 빨간 에러가 없나요?

\---

## 6\. 절대 하지 말아야 할 것

|하지마... ㅜㅠ|이유|
|-|-|
|`main`에 직접 push|검증 안 된 코드가 바로 배포.|
|`git push --force`|다른 사람의 작업을 지워버릴 수 있음.|
|`.env` 파일, API 키 커밋|한 번 올라가면 삭제해도 기록에 남음.|
|남의 브랜치를 마음대로 수정|작업이 꼬임. 필요하면 먼저 물어봐.|
|`git status` 확인 없이 `git add .`|올리면 안 되는 파일이 섞일 수 있음.|

### API 키를 실수로 올렸다면

1. 파일을 지우고, 해당 키를 **재발급**.

\---

